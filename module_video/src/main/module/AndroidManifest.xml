<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yang.module_video">

    <application
        android:name="com.yang.common_lib.base.app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.fragment.recommend.activity.SearchActivity"/>
        <activity android:name=".ui.fragment.recommend.activity.UploadFileActivity" android:theme="@style/AppTheme.MD"/>
        <activity android:name=".ui.fragment.recommend.activity.VideoPlayActivity" />
    </application>
</manifest>